config {
  schema: "curated",
  type: "table"
}

SELECT DISTINCT
  Utente AS utente,
  Dimensione AS dimensione,
  Valore AS valore,
  load_timestamp,
  CURRENT_TIMESTAMP() AS batched_at
FROM ${ref("stg_securities")}